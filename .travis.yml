language: python

sudo: required

cache: packages
install: pip install tox-travis

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install -e .
  - pip install codecov
  - pip install pytest-cov

script: tox

after_success:
  - codecov

